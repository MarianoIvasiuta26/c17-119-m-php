import { Link, Outlet, Navigate } from 'react-router-dom'

export default function SiteHeader() {
  return (
    <header className="sticky top-0 z-50 w-full border-b border-b-teal-950 bg-teal-950 text-white">
        <div className="container h-16 mx-auto px-5 flex items-center justify-between">
            <div className="flex flex-1 items-center justify-end space-x-4">
                <nav className="flex items-center space-x-4">
                    dfdfg
                </nav>
            </div>
        </div>
    </header>
  )
}
